// vercel.json（最新最稳版）
{
  "builds": [
    { "src": "api/**/*.php", "use": "vercel-php@0.6.1" }
  ],
  "routes": [
    { "src": "/admin/(.*)", "dest": "/api/$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
